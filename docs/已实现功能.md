# 已实现功能

## P0：Agent 核心可用版

### P0-1：项目骨架 + OpenHarness 裁剪 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| `resume_agent/` 包结构 | ✅ 已完成 | 包含 api/engine/config/tools/memory/prompts/models/services/skills/templates 等子模块 |
| `build_resume_runtime()` | ✅ 已完成 | 精简版 RuntimeBundle 构建，支持用户记忆注入 + Skill 加载 |
| OpenAICompatibleClient | ✅ 已完成 | DeepSeek/OpenAI 兼容客户端，流式对话，指数退避重试，429/500/502/503 自动重试 |
| ApiKeyPool 多 Key 轮询 | ✅ 已完成 | 令牌桶限流 + 轮询策略，已与 OpenAICompatibleClient 集成，429 自动报告 Key 暂停 |
| QueryEngine | ✅ 已完成 | 对话引擎，支持流式输出、工具调用循环、max_turns 控制、权限检查、Hook 前后置 |
| RuntimeBundle | ✅ 已完成 | 运行时数据类，包含 QueryEngine 及其依赖 |
| 权限检查（AUTO 模式） | ✅ 已完成 | PermissionChecker 固定 FULL_AUTO 模式，敏感路径拦截 |
| Hook 系统（空实现） | ✅ 已完成 | HookExecutor + HookRegistry，P0 阶段不加载 hooks |
| .env 配置加载 | ✅ 已完成 | python-dotenv 自动加载项目根目录 .env 文件 |
| 异常体系 | ✅ 已完成 | 统一错误码（1001-6001）+ ResumeAgentError 层次结构 |
| 上下文压缩（compact） | ✅ 已完成 | 长对话自动压缩，Token 估算 + 阈值判断 + LLM 生成摘要，在 `run_query()` 中集成 |

**关键文件**：
- `resume_agent/runtime.py` — RuntimeBundle 构建 + 进程级单例
- `resume_agent/api/openai_client.py` — OpenAI 兼容客户端
- `resume_agent/api_key_pool.py` — 多 Key 轮询池
- `resume_agent/engine/query_engine.py` — 对话引擎
- `resume_agent/engine/query.py` — 核心工具调用循环
- `resume_agent/config/settings.py` — 全局配置加载
- `resume_agent/exceptions.py` — 统一错误码与异常定义
- `resume_agent/services/compact.py` — 上下文压缩服务

### P0-2：简历领域提示词 + Skill + 记忆 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| `resume-skill.md` | ✅ 已完成 | 简历优化领域知识：ATS 友好、STAR 法则、量化成果、关键词匹配、不同场景策略 |
| 系统提示词模板 | ✅ 已完成 | 角色定义（简历优化顾问）+ 输出格式约束（Markdown 简历模板）+ 工作原则 + 工具使用规范 |
| 提示词组装逻辑 | ✅ 已完成 | `build_resume_system_prompt(user_id)` 三层组装：角色提示词 + 用户记忆 + resume-skill |
| 用户记忆加载 | ✅ 已完成 | 按 user_id 加载记忆目录，容量控制（简历 16KB/其他 4KB），自动注入系统提示词 |
| `memory_write` 工具 | ✅ 已完成 | 白名单控制（职业偏好/技能标签/优化历史），append/replace 模式，容量控制 |
| `skill_loader` 工具 | ✅ 已完成 | `SkillLoaderTool` 已在 `_get_shared_tool_registry()` 中注册，LLM 可调用加载 resume-skill.md |
| `web_fetch` 工具 | ✅ 已完成 | 抓取 URL 网页内容，5 分钟缓存，内容截断，协议校验（仅 HTTP/HTTPS） |

**已知问题**：
- ~~⚠️ `SkillLoaderTool` 已实现但**未在 `_get_shared_tool_registry()` 中注册**，导致 LLM 无法调用 `skill_loader` 工具。~~ ✅ 已修复
- ~~⚠️ `web_fetch` 使用简易正则提取 HTML 正文，对复杂页面效果不佳，后续可引入 readability-lxml。~~ ✅ 已修复：优先使用 readability-lxml，回退到 article/main 标签 + 正则

**关键文件**：
- `resume_agent/prompts/system_prompt.py` — 系统提示词 + 组装逻辑
- `resume_agent/skills/resume-skill.md` — 简历优化领域知识
- `resume_agent/skills/resume_skill.py` — Skill 管理模块
- `resume_agent/memory/manager.py` — 记忆加载/写入/容量控制
- `resume_agent/memory/paths.py` — 用户记忆目录路径管理
- `resume_agent/tools/memory_write.py` — 记忆写入工具
- `resume_agent/tools/skill_loader.py` — 技能加载工具
- `resume_agent/tools/web_fetch.py` — 网页抓取工具

### P0-3：SSE 对话端点 + 极简验证页面 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| FastAPI 应用骨架 | ✅ 已完成 | 路由注册、CORS 中间件（allow_origins=*）、静态文件挂载、生命周期管理 |
| `POST /api/chat` SSE 端点 | ✅ 已完成 | SSE 流式对话，P0 开发模式使用默认 user_id `dev_user` |
| SessionPool | ✅ 已完成 | LRU 淘汰 + 空闲超时淘汰（5 分钟定时），max_sessions 限制，会话 Key 格式 `channel:user_id:session_id` |
| StreamEvent → SSE 序列化 | ✅ 已完成 | 完整映射：text_delta/thinking_delta/tool_execution_started/tool_execution_completed/status/error/assistant_turn_complete/resume_generated/resume_data/ping/connection_timeout/session_started |
| 极简 HTML 验证页面 | ✅ 已完成 | 单文件 HTML（P1 阶段已升级为 5 Tab 页面，详见 P1 部分） |
| 会话快照持久化 | ✅ 已完成 | `session_pool._save_snapshot()` 已实现，会话淘汰/关闭时自动保存对话历史到磁盘 |

**已知问题**：
- ~~⚠️ `session_pool._save_snapshot()` 方法体为 `pass`（有 TODO 注释），`session_storage.py` 已实现但未被调用，会话淘汰或关闭时不会持久化对话历史。~~ ✅ 已修复
- ~~⚠️ SSE 心跳（`SsePing`）已在事件类型中定义，但后端 `chat.py` 未实现定期发送 ping 的逻辑，长连接可能被中间件超时断开。~~ ✅ 已修复

**关键文件**：
- `backend/app.py` — FastAPI 应用入口
- `backend/routes/chat.py` — SSE 流式对话端点
- `resume_agent/session_pool.py` — 多租户会话池
- `resume_agent/models/sse_events.py` — SSE 事件类型定义
- `resume_agent/services/session_storage.py` — 会话快照持久化
- `frontend/index.html` — 验证页面（已升级为 P1 版本）

---

## P1：Web 服务可用版

### P1-1：简历渲染与下载 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| `resume_renderer.py` | ✅ 已完成 | Markdown → PDF（fpdf2）+ HTML（python-markdown）+ Markdown 原文，渲染队列（同时仅 1 个渲染任务），60 秒超时保护 |
| `render_pdf.py` | ✅ 已完成 | 基于 fpdf2 的 Markdown → PDF 渲染，支持标题/段落/列表/水平线，模板配色（professional/academic/creative） |
| 简历快照持久化 | ✅ 已完成 | LLM 生成简历后自动保存到 `resumes/{resume_id}.md`，与对话会话解耦，每用户最多 20 份自动清理 |
| `GET /api/resume/{resume_id}/download` | ✅ 已完成 | 简历下载端点（使用 resume_id），支持 PDF/HTML/Markdown 格式 + 模板选择 |
| `GET /api/resume/{resume_id}/preview` | ✅ 已完成 | 简历预览端点，返回 HTML（带 CSS 模板） |
| `GET /api/resume/templates` | ✅ 已完成 | 获取可用简历模板列表（professional/academic/creative） |
| `GET /api/resume` | ✅ 已完成 | 列出用户简历快照 |
| `DELETE /api/resume/{resume_id}` | ✅ 已完成 | 删除简历快照 |
| 简历自动检测与保存 | ✅ 已完成 | `chat.py` 中正则检测简历结构，自动保存快照并推送 `resume_generated` SSE 事件 |
| CSS 模板 | ✅ 已完成 | professional.css（简洁商务）/ academic.css（学术风）/ creative.css（创意排版），均含 @page A4 打印设置 |

**已知问题**：
- ~~⚠️ `render_pdf.py` 硬编码 Windows 字体路径 `C:/Windows/Fonts`，跨平台兼容性差。~~ ✅ 已修复：跨平台字体搜索，支持 Windows/macOS/Linux
- ~~⚠️ PDF 渲染使用 fpdf2 而非详细设计中规划的 weasyprint（因 weasyprint 需系统级 GTK 依赖），功能基本等价但渲染精度略低。~~ ✅ P3-1 已修复：多后端渲染引擎 weasyprint → xhtml2pdf → fpdf2

**关键文件**：
- `resume_agent/resume_renderer.py` — 渲染队列 + 快照持久化
- `resume_agent/render_pdf.py` — fpdf2 Markdown → PDF 渲染
- `resume_agent/templates/` — CSS 模板目录
- `backend/routes/resume.py` — 简历 API 路由

### P1-2：记忆管理 API + 用户配置 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| `GET /api/memory` | ✅ 已完成 | 获取当前用户记忆文档列表（文件名/大小/可写标识） |
| `GET /api/memory/{doc_name}` | ✅ 已完成 | 获取指定记忆文档内容 |
| `PUT /api/memory/{doc_name}` | ✅ 已完成 | 更新记忆文档（append/replace 模式），简历原文不允许直接编辑 |
| `DELETE /api/memory/{doc_name}` | ✅ 已完成 | 删除记忆文档，简历原文不允许删除 |
| `POST /api/memory/upload` | ✅ 已完成 | 上传简历原文（支持 .md/.txt），存为 `简历原文.md`，容量控制 |
| 用户级 Settings | ✅ 已完成 | UserSettings（默认模板/语言风格/输出语言/自动保存），每用户独立配置文件 |
| `GET /api/settings` | ✅ 已完成 | 获取当前用户配置 |
| `PUT /api/settings` | ✅ 已完成 | 更新当前用户配置（支持部分更新） |
| 记忆注入提示词 | ✅ 已完成 | 对话时自动加载用户记忆文件到系统提示词 |

**已知问题**：
- ⚠️ PDF 文件上传不支持自动解析，上传 PDF 时返回错误提示。

**关键文件**：
- `backend/routes/memory.py` — 记忆 CRUD API
- `backend/routes/settings.py` — 用户配置 API
- `resume_agent/memory/manager.py` — 记忆管理核心逻辑
- `resume_agent/config/settings.py` — UserSettings 模型 + 加载/保存

### P1-3：工具与系统 API ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| `GET /api/tools` | ✅ 已完成 | 查询可用工具列表（从 ToolRegistry 读取） |
| `GET /api/mcp/status` | ✅ 已完成 | MCP 服务状态（P1 阶段返回配置信息，未实际连接 MCP） |
| `GET /api/skills` | ✅ 已完成 | Skill 列表（含名称/是否找到/预览摘要） |
| `GET /api/sessions` | ✅ 已完成 | 列出历史会话（从 session_storage 读取快照） |
| `GET /api/sessions/{id}` | ✅ 已完成 | 加载历史会话详情 |

**已知问题**：
- ⚠️ MCP 状态始终返回 `not_connected`，未实际连接任何 MCP 服务。

**关键文件**：
- `backend/routes/admin.py` — 工具/MCP/Skill/会话 API
- `resume_agent/skills/resume_skill.py` — Skill 管理模块
- `resume_agent/services/session_storage.py` — 会话快照持久化

### P1-4：前端验证页面升级 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| 对话面板 | ✅ 已完成 | SSE 流式对话，逐字渲染，工具执行通知，简历自动保存通知 |
| 记忆管理面板 | ✅ 已完成 | 记忆文档列表/查看/编辑/删除，简历原文上传 |
| 简历下载面板 | ✅ 已完成 | 简历快照列表/预览（iframe）/PDF 下载/MD 下载/模板选择 |
| 工具/技能面板 | ✅ 已完成 | 工具列表/技能列表/MCP 状态/历史会话 |
| 配置面板 | ✅ 已完成 | 默认模板/语言风格/输出语言/自动保存 |

**关键文件**：
- `frontend/index.html` — P1 验证页面（30.84 KB，5 Tab 面板）

---

## P2：多用户版

### P2-1：用户认证 + SQLite 迁移 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| SQLite 数据库 | ✅ 已完成 | `ResumeAgentDB` 管理用户认证数据、会话元数据、简历索引、IM 渠道映射 |
| JWT 认证 | ✅ 已完成 | HMAC-SHA256 签名，access_token 7天有效期，refresh_token 30天有效期 |
| `POST /api/auth/register` | ✅ 已完成 | 用户注册（用户名唯一，密码 pbkdf2 哈希），自动初始化用户目录 |
| `POST /api/auth/login` | ✅ 已完成 | 用户登录，返回 access_token + refresh_token |
| `POST /api/auth/refresh` | ✅ 已完成 | 刷新 Token，使用 refresh_token 获取新的 access_token |
| `GET /api/auth/profile` | ✅ 已完成 | 获取当前用户信息（需 JWT 认证） |
| 认证中间件 | ✅ 已完成 | `AuthMiddleware` 拦截所有 `/api/*` 请求（`/api/auth/*` 除外），验证 JWT 后注入 `request.state.user_id` |
| 开发模式移除 | ✅ 已完成 | 移除 `default_user_id` / `effective_default_user_id`，所有路由从 JWT 获取 user_id |
| PDF 上传解析 | ✅ 已完成 | 使用 PyPDF2 提取 PDF 文本，上传 PDF 简历时自动解析并存为 `简历原文.md` |

**关键文件**：
- `resume_agent/db.py` — SQLite 数据库管理（进程级单例）
- `backend/middleware/auth.py` — JWT 认证中间件 + JWT 工具函数（创建/验证/密码哈希）
- `backend/routes/auth.py` — 用户认证 API（注册/登录/刷新/个人信息）
- `backend/app.py` — 更新：注册 auth 路由 + AuthMiddleware + 数据库生命周期

**已知问题**：
- ~~🟢 低~~ | ~~MCP 未实际连接~~ | ~~MCP 工具不可用~~ | ✅ P3-3 已修复：MCP 客户端框架已实现

### P2-2：多租户隔离 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| 用户级 Memory 隔离 | ✅ 已完成 | 按 `user_id` 隔离记忆目录，用户 A 看不到用户 B 的记忆 |
| 会话隔离 | ✅ 已完成 | 每用户独立会话列表和消息历史，路径按 `user_id` 隔离 |
| 简历隔离 | ✅ 已完成 | 简历快照按 `user_id` 存储在不同目录，无法跨用户访问 |
| memory_write 工具隔离 | ✅ 已完成 | 移除 `dev_user` 回退，强制从 `context.metadata` 获取 `user_id`，无 user_id 则拒绝写入 |
| MCP 认证动态注入 | ✅ 已完成 | `mcp_auth.py` 按用户动态注入 MCP 工具认证信息（全局 headers + 用户级 headers 合并） |
| SSE 认证适配 | ✅ 已完成 | SSE 请求通过 `Authorization` header 传递 JWT，`AuthMiddleware` 统一处理 |
| 会话元数据同步 SQLite | ✅ 已完成 | `save_session_snapshot` 保存时同步写入 `session_meta` 表 |
| 简历索引同步 SQLite | ✅ 已完成 | `save_resume_snapshot` 保存时同步写入 `resume_index` 表 |

**关键文件**：
- `resume_agent/mcp_auth.py` — 用户级 MCP 认证动态注入（`get_mcp_headers` / `load_user_mcp_auth` / `save_user_mcp_auth`）
- `resume_agent/services/session_storage.py` — 更新：会话保存时同步 SQLite 元数据
- `resume_agent/resume_renderer.py` — 更新：简历保存时同步 SQLite 索引
- `resume_agent/tools/memory_write.py` — 更新：移除 `dev_user` 回退，强制 user_id

### P2-3：Web 前端正式版 ✅

| 功能 | 状态 | 说明 |
|------|------|------|
| Next.js 16 项目搭建 | ✅ 已完成 | App Router + TypeScript + Tailwind CSS v4 + shadcn/ui (Base UI) |
| API 通信层 | ✅ 已完成 | ofetch 封装 + JWT 认证实例 + SSE 流式对话 + 文件下载 |
| 状态管理 | ✅ 已完成 | Zustand (auth + chat store) + @tanstack/react-query |
| 登录/注册页面 | ✅ 已完成 | react-hook-form + zod 表单校验，JWT 持久化 (localStorage) |
| Landing Page | ✅ 已完成 | 产品介绍 + 功能展示 + CTA |
| 对话界面 | ✅ 已完成 | SSE 流式对话 + Markdown 渲染 + 工具调用展示 + 简历生成按钮 + 思考过程展示 + 头像 + Enter发送 + 自动滚动 + session_id 维持会话连续性 + 历史会话列表与恢复 + zustand persist 持久化 |
| 简历管理页面 | ✅ 已完成 | 列表 + 详情预览 + PDF/Markdown 下载 + 删除 |
| 记忆管理页面 | ✅ 已完成 | 文件列表 + 内容查看/编辑 + 简历上传 |
| 设置页面 | ✅ 已完成 | 账户信息 + 修改密码 |
| 响应式布局 | ✅ 已完成 | 桌面侧边栏 + 移动端 Sheet 抽屉 |
| 认证守卫 | ✅ 已完成 | AuthGuard 组件 + 未认证重定向 |
| 后端集成 | ✅ 已完成 | FastAPI CORS 已配置，移除 StaticFiles 挂载，改由 Nginx 反代 |

**前端技术栈**：
- Next.js 16 (App Router) + React 19 + TypeScript
- Tailwind CSS v4 + shadcn/ui (Base UI)
- ofetch + Zustand + @tanstack/react-query
- react-hook-form + zod + react-markdown

**关键文件**：
- `frontend/src/lib/api.ts` — API 通信层（ofetch + SSE 流式）
- `frontend/src/store/auth.ts` — JWT 认证状态
- `frontend/src/store/chat.ts` — 对话消息状态
- `frontend/src/app/(app)/layout.tsx` — 应用外壳（侧边栏 + 导航）
- `frontend/src/app/(app)/chat/page.tsx` — 对话页面
- `frontend/src/app/(app)/resumes/` — 简历管理
- `frontend/src/app/(app)/memory/page.tsx` — 记忆管理

---

## P3：质量增强版

### P3-1：简历结构化渲染引擎 ✅ 已完成

> **架构升级**：从 `python-markdown + CSS overlay` 升级为**结构化数据驱动**的渲染管线：`Markdown → ResumeData → Jinja2 HTML 模板 → PDF`，每套模板是完整的 HTML+CSS 文件，布局完全自由。同时新增 `resume_data` SSE 事件和前端 `ResumePreview` 组件，实现所见即所得预览。

| 功能 | 状态 | 说明 |
|------|------|------|
| ResumeData 数据模型 | ✅ 已完成 | Pydantic 结构化简历数据模型（姓名/联系方式/工作经历/教育/技能/项目） |
| Markdown → ResumeData 解析器 | ✅ 已完成 | 容错解析 LLM 输出，支持标题别名映射、联系方式正则提取、空条目过滤 |
| Jinja2 HTML 模板引擎 | ✅ 已完成 | 替代 python-markdown + CSS overlay，ResumeData 驱动模板渲染 |
| professional 模板 | ✅ 已完成 | 双栏侧边栏布局（深色侧边栏+白色主内容区），适用于互联网/科技 |
| academic 模板 | ✅ 已完成 | 传统单栏居中布局（居中姓名+装饰线），教育背景优先，适用于高校/研究所 |
| creative 模板 | ✅ 已完成 | 卡片式布局（渐变 Hero 头部+圆角卡片+技能标签），适用于设计/市场 |
| SSE resume_data 事件 | ✅ 已完成 | LLM 输出完毕后推送结构化 ResumeData JSON + template_hint |
| 前端 ResumePreview 组件 | ✅ 已完成 | React 组件接收 ResumeData 渲染三套模板，与 PDF 视觉一致 |
| 前端 SSE 事件处理 | ✅ 已完成 | 流式 Markdown → 收到 resume_data → 自动升级为组件渲染 |
| 双格式快照持久化 | ✅ 已完成 | 同时保存 ResumeData JSON + Markdown 原文，旧快照自动解析兼容 |
| GET /api/resume/{id}/data | ✅ 已完成 | 获取简历结构化数据，前端组件渲染使用 |
| ChatStore resumeData 支持 | ✅ 已完成 | ChatMessage 新增 resumeData/templateHint 字段，setResumeDataOnLastMessage action |
| 渲染引擎多后端降级 | ✅ 已完成 | weasyprint → xhtml2pdf → fpdf2 逐级降级，结构化渲染优先 |
| 旧 Markdown 渲染兼容 | ✅ 已完成 | 旧 CSS 模板和 python-markdown 渲染路径保留，作为降级后备 |
| 简历输出标记分隔机制 | ✅ 已完成 | LLM 使用 `<!--RESUME-->` / `<!--/RESUME-->` 标记包裹简历正文；截取逻辑双路径：标记提取（主路径）+ 白名单过滤（降级路径）；流式推送自动过滤标记标签 |
| 简历预览模板切换 | ✅ 已完成 | 前端 ResumePreview 支持商务/学术/创意三种模板实时切换，默认使用后端推荐的 template_hint |
| 优化建议独立展示 | ✅ 已完成 | 标记外的建议内容通过 SSE suggestions 字段推送，前端在简历预览下方以 Markdown 形式展示；标记前的引导语通过 resume_prefix 字段推送，前端在预览上方展示 |

**已知问题**：
- 🟡 中 | weasyprint 在 Windows 上需 GTK 运行时 | Windows 环境降级到 xhtml2pdf | 可接受，Linux 部署不受影响 |
- 🟢 低 | xhtml2pdf CSS 支持有限（CSS 2.1 子集） | 部分 CSS3 特性（如 flexbox）不生效 | weasyprint 部署时不受影响 |
- 🟢 低 | creative 模板卡片阴影在 xhtml2pdf 中不渲染 | 打印效果略有差异 | weasyprint 部署时不受影响 |

**关键文件**：
- `resume_agent/models/resume_data.py` — 简历结构化数据模型（ResumeData + 子模型）
- `resume_agent/resume_parser.py` — Markdown → ResumeData 解析器（容错解析）
- `resume_agent/render_pdf_engine.py` — 多后端渲染引擎（新增 Jinja2 渲染路径）
- `resume_agent/resume_renderer.py` — 渲染队列 + 双格式快照持久化
- `resume_agent/templates/professional.html` — 简洁商务模板（双栏侧边栏）
- `resume_agent/templates/academic.html` — 学术风模板（传统单栏）
- `resume_agent/templates/creative.html` — 创意排版模板（卡片式）
- `resume_agent/models/sse_events.py` — SSE 事件类型（新增 SseResumeData）
- `backend/routes/chat.py` — SSE 对话端点（新增 resume_data 事件推送 + 标记分隔机制 + 流式标记过滤）

- `backend/routes/resume.py` — 简历 API（新增 /data 端点）
- `frontend/src/components/resume-preview.tsx` — 前端简历预览组件
- `frontend/src/store/chat.ts` — 对话状态（新增 ResumeData 类型和 setResumeDataOnLastMessage）
- `frontend/src/app/(app)/chat/page.tsx` — 对话页面（处理 resume_data 事件 + ResumePreview 集成）

### P3-2：领域 Skill 精调 + 智能模板匹配 ✅ 已完成

> **能力升级**：从通用简历优化升级为**行业感知**的智能简历优化系统。新增行业细分技能文件、JD 解析技能、动态技能加载逻辑、行业→模板自动映射、智能模板推荐 API。系统提示词和 resume-skill.md 大幅迭代优化，增加完整输出示例和格式禁止事项。

| 功能 | 状态 | 说明 |
|------|------|------|
| `resume-skill.md` 迭代 | ✅ 已完成 | 大幅优化：增加联系方式格式规范、技能格式规范、项目经历格式规范、正确/错误示例对比、输出质量检查清单 |
| `resume-tech.md` 行业技能 | ✅ 已完成 | 互联网/科技行业专项技能：前后端/算法/AI/产品运营关键词分类、量化成果维度、技术栈匹配策略、JD 术语解读、不同职位层级策略 |
| `resume-finance.md` 行业技能 | ✅ 已完成 | 金融行业专项技能：银行/证券/保险/金融科技关键词分类、专业资质展示要求、金融指标量化、合规风控要求、不同职位层级策略 |
| `resume-jd.md` JD 解析技能 | ✅ 已完成 | JD 解析框架：基础信息/硬技能/软技能/行业术语/隐性要求提取、行业识别策略、JD 解析输出格式、匹配度评估方法 |
| 系统提示词优化 | ✅ 已完成 | 增加完整输出示例、格式禁止事项（6 条）、JD 分析要求、真实性约束、新增 skill_loader 工具说明 |
| 动态技能加载 | ✅ 已完成 | `build_resume_system_prompt()` 根据 `latest_user_prompt` 推断行业，动态注入行业技能文件；新增 `load_industry_skill()` 和 `load_jd_skill()` |
| 行业→模板映射 | ✅ 已完成 | `INDUSTRY_TEMPLATE_MAP` 14 个行业→模板映射 + `JOB_KEYWORD_INDUSTRIES` 60+ 岗位关键词→行业映射；`detect_industry_from_text()` 关键词频率分析推断行业 |
| 智能模板推荐 | ✅ 已完成 | `get_template_hint()` 根据行业或 JD 文本推荐最佳模板；SSE `resume_data` 事件的 `template_hint` 从硬编码改为动态推荐 |
| 智能模板推荐 API | ✅ 已完成 | `GET /api/resume/template-hint?jd=...&industry=...` 根据岗位描述或行业标识推荐模板，返回推荐模板、识别行业、行业映射表 |
| Skill 分类标签 | ✅ 已完成 | `resume_skill.py` 新增 `_SKILL_CATEGORIES` 分类标签，`list_skills()` 返回结果包含 `category` 字段 |
| 运行时参数传递 | ✅ 已完成 | `build_resume_runtime()` + `ResumeSessionPool.get_or_create()` 新增 `latest_user_prompt` 参数，支持将用户 prompt 传递到提示词组装层 |

**关键文件**：
- `resume_agent/skills/resume-skill.md` — 通用简历优化技能（大幅迭代）
- `resume_agent/skills/resume-tech.md` — 互联网/科技行业专项技能
- `resume_agent/skills/resume-finance.md` — 金融行业专项技能
- `resume_agent/skills/resume-jd.md` — JD 解析专项技能
- `resume_agent/prompts/system_prompt.py` — 系统提示词优化 + 动态技能加载
- `resume_agent/resume_renderer.py` — 行业识别 + 模板推荐 + 行业→技能文件映射
- `resume_agent/skills/resume_skill.py` — Skill 管理模块（新增分类标签）
- `resume_agent/runtime.py` — 新增 `latest_user_prompt` 参数
- `resume_agent/session_pool.py` — 传递 `latest_user_prompt` 到运行时
- `backend/routes/chat.py` — SSE `template_hint` 改为动态推荐 + 传递 `latest_user_prompt`
- `backend/routes/resume.py` — 新增 `GET /resume/template-hint` API

### P3-3：MCP 工具接入 ✅ 已完成

> **架构升级**：引入 MCP (Model Context Protocol) 客户端框架，将 PDF 渲染和邮件发送等重量级操作隔离到独立 HTTP MCP 服务中。McpClientManager 作为进程级单例管理所有 MCP 连接，MCP 工具通过 ToolWrapper 动态注册到 ToolRegistry，支持用户级认证注入。

| 功能 | 状态 | 说明 |
|------|------|------|
| McpClientManager 共享单例 | ✅ 已完成 | 进程级单例，管理所有 HTTP MCP 连接，工具发现/注册/刷新/关闭；`get_mcp_manager()` 全局访问 |
| McpHttpClient | ✅ 已完成 | 单个 HTTP MCP 服务器客户端，支持连接/断开/工具发现/工具调用/健康检查；httpx 连接复用 |
| McpToolWrapper | ✅ 已完成 | BaseTool 子类，将远程 MCP 工具注册到 ToolRegistry；动态创建 Pydantic 输入模型；自动注入用户级认证 headers |
| PDF 转换 MCP 服务 | ✅ 已完成 | 独立 HTTP MCP 服务（`mcp_servers/pdf_server/`），基于 weasyprint 渲染 PDF；MCP 协议端点：`/health`、`/tools/list`、`/tools/call` |
| 邮件发送 MCP 服务 | ✅ 已完成 | 独立 HTTP MCP 服务（`mcp_servers/email_server/`），SMTP 发送邮件（支持 HTML 正文+附件+抄送/密送）；STARTTLS/SSL/无加密三种模式 |
| 运行时 MCP 集成 | ✅ 已完成 | `init_mcp_tools()` 在应用启动时初始化 MCP 连接并注册工具；`shutdown_mcp()` 在应用关闭时清理；ToolRegistry 新增 `unregister()` 方法 |
| 应用生命周期管理 | ✅ 已完成 | `app.py` lifespan 中初始化/关闭 MCP；MCP 连接失败不影响主服务启动 |
| MCP 服务器默认配置 | ✅ 已完成 | `DEFAULT_MCP_SERVERS` 配置 pdf（localhost:9100）和 email（localhost:9101）；`McpServerConfig` 新增 `enabled` 字段；`load_settings()` 自动合并默认配置 |
| MCP 状态 API | ✅ 已完成 | `GET /api/mcp/status` 返回实际连接状态、已发现工具列表；降级返回配置信息 |
| MCP 刷新 API | ✅ 已完成 | `POST /api/mcp/refresh` 重新初始化 MCP 连接并注册工具 |
| MCP 认证 API | ✅ 已完成 | `GET/PUT/DELETE /api/mcp/auth/{server_name}` 管理用户级 MCP 认证信息；GET 自动隐藏敏感值 |
| 系统提示词更新 | ✅ 已完成 | 工具使用规范中新增 `mcp__pdf__convert` 和 `mcp__email__send` 说明 |

**关键文件**：
- `resume_agent/mcp/__init__.py` — MCP 包初始化
- `resume_agent/mcp/client.py` — MCP HTTP 客户端（McpHttpClient + McpToolInfo + McpCallResult）
- `resume_agent/mcp/tool.py` — MCP 工具包装器（McpToolWrapper + 动态模型创建）
- `resume_agent/mcp/manager.py` — MCP 客户端管理器（McpClientManager + 进程级单例）
- `resume_agent/runtime.py` — 新增 `init_mcp_tools()` / `shutdown_mcp()` + MCP 工具注册
- `resume_agent/tools/base.py` — ToolRegistry 新增 `unregister()` 方法
- `resume_agent/config/settings.py` — McpServerConfig 新增 `enabled` 字段 + `DEFAULT_MCP_SERVERS` 默认配置
- `resume_agent/prompts/system_prompt.py` — 系统提示词新增 MCP 工具说明
- `backend/app.py` — 应用生命周期中初始化/关闭 MCP
- `backend/routes/admin.py` — 更新 MCP 状态 API + 新增认证配置 API + 刷新 API
- `mcp_servers/pdf_server/main.py` — PDF 转换 HTTP MCP 服务
- `mcp_servers/email_server/main.py` — 邮件发送 HTTP MCP 服务

### P3-4：速率限制 + 监控 ✅ 已完成

> **架构升级**：引入用户级 API 速率限制和基础监控体系。RateLimitMiddleware 按 user_id 维护令牌桶，对 `/api/chat` 端点严格限制、其他 API 宽松限制；MonitoringMiddleware 收集请求量/延迟/错误率指标，定期输出汇总日志；DeepSeek 429 异常在 SSE 流中发送友好中文提示。

| 功能 | 状态 | 说明 |
|------|------|------|
| UserRateLimiter 令牌桶 | ✅ 已完成 | 按 user_id 维护独立令牌桶，支持阻塞 `acquire()` 和非阻塞 `try_acquire()`，自动清理旧桶，`get_status()` 查询用户状态 |
| RateLimitMiddleware | ✅ 已完成 | 用户级 API 速率限制中间件；`/api/chat` 完整限制，其他 API 端点 3 倍宽松；公开路径和 OPTIONS 放行；429 响应含 Retry-After header |
| 速率限制配置 | ✅ 已完成 | `rate_limit_enabled` / `rate_limit_rpm` / `rate_limit_max_wait` 三项配置，通过 settings.json 或环境变量设置 |
| DeepSeek 429 SSE 友好提示 | ✅ 已完成 | `RateLimitFailure` 异常发送中文 SSE 状态提示 + 错误事件；`RateLimitError` 异常同样返回友好提示 |
| API 重试状态推送 | ✅ 已完成 | `ApiRetryEvent` 转换为中文 `StatusEvent`，通过 SSE 推送"AI 服务暂时繁忙，正在自动重试"提示 |
| MonitoringMiddleware | ✅ 已完成 | 基础监控中间件，收集 total_requests / error_requests / avg_latency / max_latency / 按路径统计 / 按状态码统计；`get_metrics()` 供 API 查询 |
| 监控定期日志 | ✅ 已完成 | 定期输出汇总日志（请求量/错误率/平均延迟/热点路径 Top5），应用生命周期中启动/停止 |
| 监控配置 | ✅ 已完成 | `monitor_enabled` / `monitor_log_interval` 两项配置 |
| 速率限制状态 API | ✅ 已完成 | `GET /api/rate-limit/status` 返回当前用户的速率限制状态（enabled / rpm_limit / tokens_available / limited） |
| 监控指标 API | ✅ 已完成 | `GET /api/monitor/metrics` 返回监控指标（需 JWT 认证） |
| .env.example 更新 | ✅ 已完成 | 新增速率限制和监控配置说明 |

**关键文件**：
- `resume_agent/services/rate_limiter.py` — 用户级令牌桶限速器（UserRateLimiter + UserBucket）
- `backend/middleware/rate_limit.py` — 速率限制中间件（RateLimitMiddleware）
- `backend/middleware/monitoring.py` — 监控中间件（MonitoringMiddleware + _RequestMetrics）
- `backend/app.py` — 中间件注册 + 生命周期管理 + getter 函数
- `backend/routes/chat.py` — DeepSeek 429 SSE 友好提示
- `backend/routes/admin.py` — 新增速率限制状态 API + 监控指标 API
- `resume_agent/config/settings.py` — 新增速率限制和监控配置项
- `resume_agent/engine/query.py` — ApiRetryEvent 中文提示

---

## 全局已知问题汇总

| 优先级 | 问题 | 影响 | 状态 |
|--------|------|------|------|
| ~~🔴 高~~ | ~~`SkillLoaderTool` 未在 ToolRegistry 中注册~~ | ~~LLM 无法调用 `skill_loader` 工具~~ | ✅ 已修复 |
| ~~🔴 高~~ | ~~`ApiKeyPool` 未与 `OpenAICompatibleClient` 集成~~ | ~~多 Key 轮询功能空转，仅使用第一个 Key~~ | ✅ 已修复 |
| ~~🟡 中~~ | ~~`session_pool._save_snapshot()` 空实现~~ | ~~会话淘汰/关闭时不持久化对话历史~~ | ✅ 已修复 |
| ~~🟡 中~~ | ~~上下文压缩（compact）未实现~~ | ~~长对话无自动压缩，可能超出 Token 限制~~ | ✅ 已修复 |
| ~~🟡 中~~ | ~~SSE 心跳未实现~~ | ~~长连接可能被中间件超时断开~~ | ✅ 已修复 |
| ~~🟢 低~~ | ~~`render_pdf.py` 硬编码 Windows 字体路径~~ | ~~跨平台兼容性差~~ | ✅ 已修复 |
| ~~🟢 低~~ | ~~`web_fetch` 简易正则提取 HTML~~ | ~~复杂页面抓取效果不佳~~ | ✅ 已修复 |
| ~~🟢 低~~ | ~~PDF 上传不支持解析~~ | ~~用户无法直接上传 PDF 简历~~ | ✅ 已修复 |
| ~~🟢 低~~ | ~~PDF 渲染使用 fpdf2（排版精度低）~~ | ~~简历排版对不齐、无表格/粗体支持~~ | ✅ P3-1 已修复：切换到 xhtml2pdf/weasyprint |
| ~~🟢 低~~ | ~~MCP 未实际连接~~ | ~~MCP 工具不可用~~ | ✅ P3-3 已修复：MCP 客户端框架已实现 |

---

## P4：体验优化版

### P4-1：简历评分 ✅ 已完成

> **能力升级**：简历生成后自动进行多维度评分（结构完整性/内容充实度/量化数据/关键词匹配/格式规范），通过 SSE 事件实时推送到前端，前端展示评分卡片（总分+维度条形图+改进建议+JD关键词匹配）。支持独立 API 端点对已有简历评分。

| 功能 | 状态 | 说明 |
|------|------|------|
| ResumeScorer 评分引擎 | ✅ 已完成 | 5 维度加权评分：结构完整性(20%) + 内容充实度(25%) + 量化数据(20%) + 关键词匹配(20%) + 格式规范(15%) |
| 结构完整性评分 | ✅ 已完成 | 检查必要章节（个人简介/工作经历/教育背景/技能）和推荐章节（项目经历），给出缺失提示 |
| 内容充实度评分 | ✅ 已完成 | 评估简介长度、工作经历要点数、技能标签数，给出具体改进建议 |
| 量化数据评分 | ✅ 已完成 | 检测工作经历中包含量化数据的要点占比（百分比/金额/排名/动词+数字），建议提升至 60% 以上 |
| JD 关键词匹配评分 | ✅ 已完成 | 从 JD 提取技术/技能/要求关键词，与简历文本匹配，报告已匹配和缺失关键词 |
| 格式规范评分 | ✅ 已完成 | 检查联系方式完整性、时间段格式、模糊表述（"参与/负责…工作"）等 |
| SSE resume_score 事件 | ✅ 已完成 | 简历生成后自动评分并推送 `resume_score` SSE 事件（总分/维度/建议/关键词） |
| `POST /api/resume/{id}/score` | ✅ 已完成 | 独立评分 API，支持传入 JD 文本进行关键词匹配评分 |
| SseResumeScore 事件类型 | ✅ 已完成 | 新增 `SseResumeScore` 数据类，包含 score/dimensions/suggestions/jd_keywords_matched/jd_keywords_missing |
| 前端 ResumeScoreCard 组件 | ✅ 已完成 | 评分卡片组件：总分+等级标签+5维度进度条+改进建议列表+关键词标签（已匹配/缺失） |
| ChatStore resumeScore 支持 | ✅ 已完成 | ChatMessage 新增 resumeScore 字段，setResumeScoreOnLastMessage action |
| SSE 事件处理 | ✅ 已完成 | 前端 chat/page.tsx 处理 resume_score 事件，更新消息状态 |

**关键文件**：
- `resume_agent/services/resume_scorer.py` — 简历评分引擎（ResumeScorer + 5 维度评分 + JD 关键词提取）
- `resume_agent/models/sse_events.py` — 新增 SseResumeScore 事件类型
- `backend/routes/chat.py` — 简历生成后自动评分 + SSE 推送
- `backend/routes/resume.py` — 新增 `POST /resume/{id}/score` 评分 API
- `frontend/src/components/resume-score-card.tsx` — 评分卡片组件
- `frontend/src/store/chat.ts` — ChatMessage 新增 ResumeScoreData + setResumeScoreOnLastMessage
- `frontend/src/app/(app)/chat/page.tsx` — 处理 resume_score 事件 + 集成 ResumeScoreCard

### P4-2：多轮优化 ✅ 已完成

> **能力升级**：支持对话中逐步修改简历，系统提示词增加多轮优化规则引导 LLM 增量修改；前端新增 ResumeDiffView 差异对比组件，在优化时展示新增/删除/不变的行级差异；ChatMessage 新增 prevResumeContent 字段自动保存上一版内容。

| 功能 | 状态 | 说明 |
|------|------|------|
| 多轮优化提示词 | ✅ 已完成 | 系统提示词新增「多轮优化规则」：增量修改、保留亮点、标注修改、完整输出、响应反馈、追踪偏好 |
| 前端差异对比组件 | ✅ 已完成 | `ResumeDiffView` 组件：行级 diff 算法，展示新增(绿)/删除(红)/不变行，可展开/收起，显示修改统计 |
| ChatMessage prevResumeContent | ✅ 已完成 | 多轮优化时自动保存上一版简历 Markdown，用于差异对比 |
| 差异对比集成 | ✅ 已完成 | MessageBubble 中检测到 prevResumeContent 时自动展示 ResumeDiffView |

**关键文件**：
- `resume_agent/prompts/system_prompt.py` — 系统提示词新增多轮优化规则
- `frontend/src/components/resume-diff-view.tsx` — 差异对比组件（行级 diff + 折叠展示）
- `frontend/src/store/chat.ts` — 新增 prevResumeContent 字段 + _resumeDataToMarkdown 辅助函数
- `frontend/src/app/(app)/chat/page.tsx` — 集成 ResumeDiffView + 传递 prevResumeContent

### P4-3：API Client 连接池调优 ✅ 已完成

> **性能优化**：修复 OpenAICompatibleClient 多 Key 轮询时的 httpx 连接泄露问题，改为为每个 Key 预创建独立 AsyncOpenAI 客户端并共享 httpx 连接池；MCP HTTP 客户端添加连接池配置；应用关闭时正确释放连接池资源。

| 功能 | 状态 | 说明 |
|------|------|------|
| 多 Key 预创建客户端 | ✅ 已完成 | 为每个 API Key 预创建 AsyncOpenAI 客户端，轮询时直接切换引用，不再每次重建 |
| 共享 httpx 连接池 | ✅ 已完成 | 所有 AsyncOpenAI 客户端共享一个 httpx.AsyncClient，配置连接池大小和 keep-alive 参数 |
| 连接池配置项 | ✅ 已完成 | `httpx_pool_max_connections` / `httpx_pool_max_keepalive` / `httpx_connect_timeout` 三项配置 |
| MCP HTTP 客户端优化 | ✅ 已完成 | McpHttpClient._get_client() 使用 settings 中的连接池配置 |
| 生命周期管理 | ✅ 已完成 | 应用关闭时调用 `close_shared_api_client()` 释放 httpx 连接池 |

**关键文件**：
- `resume_agent/api/openai_client.py` — 重构为多客户端复用模式 + 共享 httpx 连接池 + aclose() 方法
- `resume_agent/config/settings.py` — 新增 httpx 连接池配置项
- `resume_agent/runtime.py` — 传递连接池参数 + close_shared_api_client() 关闭方法
- `resume_agent/mcp/client.py` — _get_client() 使用 settings 配置
- `backend/app.py` — lifespan 中关闭 API 客户端连接池

### P4-4：招聘网站 JD 抓取 MCP ✅ 已完成

> **能力升级**：新增 JD 抓取 MCP 服务（`mcp_servers/jd_scraper/`），自动抓取招聘网站职位描述，支持 Boss直聘、拉勾网、猎聘、前程无忧、智聘等主流招聘网站的结构化解析，非支持网站自动降级为通用 HTML 正文提取。系统提示词引导 LLM 优先使用 JD 抓取工具。

| 功能 | 状态 | 说明 |
|------|------|------|
| JD 抓取 MCP 服务 | ✅ 已完成 | 独立 HTTP MCP 服务（`mcp_servers/jd_scraper/`），端口 9102 |
| 多网站适配解析 | ✅ 已完成 | Boss直聘/拉勾网/猎聘/前程无忧/智联招聘专属正则解析 + 通用降级提取 |
| scrape_jd 工具 | ✅ 已完成 | MCP 工具 `scrape_jd`，参数：url + max_length，返回 Markdown 格式 JD 描述 |
| 网站自动检测 | ✅ 已完成 | 根据 URL 域名自动检测招聘网站类型，选择对应解析器 |
| 通用降级提取 | ✅ 已完成 | 非支持网站自动降级为通用 HTML 正文提取（移除导航/脚本/样式等噪声） |
| MCP 默认配置 | ✅ 已完成 | `DEFAULT_MCP_SERVERS` 新增 `jd` 条目（localhost:9102） |
| 系统提示词更新 | ✅ 已完成 | 工具使用规范新增 `mcp__jd__scrape_jd` 说明，优先于 web_fetch 使用 |

**关键文件**：
- `mcp_servers/jd_scraper/main.py` — JD 抓取 HTTP MCP 服务
- `mcp_servers/jd_scraper/requirements.txt` — 依赖清单
- `mcp_servers/jd_scraper/README.md` — 服务说明
- `resume_agent/config/settings.py` — DEFAULT_MCP_SERVERS 新增 jd 条目
- `resume_agent/prompts/system_prompt.py` — 新增 mcp__jd__scrape_jd 工具说明

---

## P5 优化迭代（后续优化 P1 优先级）

### P1-5：Skill 规范落地 ✅ 已完成

> **架构升级**：Skill 系统从硬编码管理升级为**规范驱动**的可扩展体系。引入 YAML Front Matter 元数据规范，支持技能分类/标签/行业/依赖/Token 预算等结构化描述；行业→技能映射从硬编码改为从 Front Matter 的 `industry` 字段自动构建；`SkillLoaderTool` 支持加载所有已注册技能；新增 `extra_skill_dirs` 配置支持外部技能目录；提供 `validate_skill_file()` 校验工具。

| 功能 | 状态 | 说明 |
|------|------|------|
| YAML Front Matter 规范 | ✅ 已完成 | 技能文件支持 `name/version/category/tags/industry/depends/token_budget/author/description` 元数据 |
| 4 个技能文件添加 Front Matter | ✅ 已完成 | `resume-skill.md`/`resume-tech.md`/`resume-finance.md`/`resume-jd.md` 全部添加 Front Matter |
| SkillMeta 数据模型 | ✅ 已完成 | 统一元数据模型，支持 `validate()` 校验 |
| 简易 YAML 解析器 | ✅ 已完成 | 无 pyyaml 依赖，支持 string/list/int 类型解析 |
| 行业→技能自动映射 | ✅ 已完成 | `get_industry_skill_map()` 从 Front Matter `industry` 字段自动构建，替代硬编码 |
| SkillLoaderTool 扩展 | ✅ 已完成 | 支持加载所有已注册技能（不再限制为 `resume-skill`），列出可用技能名 |
| extra_skill_dirs 配置 | ✅ 已完成 | `settings.py` 新增 `extra_skill_dirs` 字段，支持外部技能目录加载 |
| 技能文件校验 | ✅ 已完成 | `validate_skill_file()` 校验 Front Matter 完整性、名称格式、文件大小、正文结构、依赖存在性 |
| load_skill_content API | ✅ 已完成 | 加载技能正文（自动去除 Front Matter），替代直接文件读取 |
| 行业映射自动构建 | ✅ 已完成 | `get_industry_skill_name()` 改用 `get_industry_skill_map()` 动态查找 |

**关键文件**：
- `resume_agent/skills/resume_skill.py` — 重写：SkillMeta + Front Matter 解析 + 目录扫描 + 校验 + 行业映射
- `resume_agent/skills/resume-skill.md` — 添加 Front Matter
- `resume_agent/skills/resume-tech.md` — 添加 Front Matter
- `resume_agent/skills/resume-finance.md` — 添加 Front Matter
- `resume_agent/skills/resume-jd.md` — 添加 Front Matter
- `resume_agent/tools/skill_loader.py` — 重写：支持所有已注册技能 + category/is_read_only_default
- `resume_agent/resume_renderer.py` — `get_industry_skill_name()` 改用动态映射
- `resume_agent/prompts/system_prompt.py` — 技能加载函数改用新 API + skill_loader 描述更新
- `resume_agent/config/settings.py` — 新增 `extra_skill_dirs` 配置

### P1-6：MCP 规范落地 ✅ 已完成

> **架构升级**：MCP 系统从简单 HTTP 客户端升级为**规范驱动**的可扩展体系。引入 MCP ToolSpec `annotations` 元数据规范，`is_read_only()` 从名称魔法推断改为读取 `annotations.readOnlyHint`；`McpServerConfig` 扩展 `description`/`auto_reconnect`/`health_check_interval` 字段；新增 `McpServerBase` 共享框架，第三方只需实现工具逻辑即可快速搭建 MCP 服务器；3 个现有 MCP 服务器全部添加 annotations。

| 功能 | 状态 | 说明 |
|------|------|------|
| McpToolAnnotations 模型 | ✅ 已完成 | 工具注解元数据：readOnlyHint/destructiveHint/idempotentHint/category/timeout_ms |
| McpToolInfo 扩展 | ✅ 已完成 | 新增 `annotations` 字段，discover_tools() 解析 annotations |
| is_read_only 从 annotations 读取 | ✅ 已完成 | `McpToolWrapper.is_read_only()` 改为读取 `annotations.readOnlyHint`，不再名称魔法推断 |
| McpToolWrapper category 属性 | ✅ 已完成 | 从 annotations.category 读取工具分类标签 |
| McpServerConfig 扩展 | ✅ 已完成 | 新增 `description`/`auto_reconnect`/`health_check_interval` 字段 |
| McpServerBase 共享框架 | ✅ 已完成 | 基类自动实现 /health、/tools/list、/tools/call 端点，子类只需 register_tool + handler |
| PDF MCP 服务器 annotations | ✅ 已完成 | convert 工具：readOnlyHint=true, category=文件转换, timeout_ms=60000 |
| Email MCP 服务器 annotations | ✅ 已完成 | send 工具：readOnlyHint=false, category=邮件发送, timeout_ms=30000 |
| JD MCP 服务器 annotations | ✅ 已完成 | scrape_jd 工具：readOnlyHint=true, category=网络抓取, timeout_ms=30000 |

**关键文件**：
- `resume_agent/mcp/client.py` — 新增 McpToolAnnotations + discover_tools 解析 annotations
- `resume_agent/mcp/tool.py` — is_read_only 改用 annotations + category/is_read_only_default 属性
- `resume_agent/mcp/server_base.py` — 新增 McpServerBase 共享框架
- `resume_agent/config/settings.py` — McpServerConfig 新增 description/auto_reconnect/health_check_interval
- `mcp_servers/pdf_server/main.py` — 添加 annotations
- `mcp_servers/email_server/main.py` — 添加 annotations
- `mcp_servers/jd_scraper/main.py` — 添加 annotations

### P1-7：模板规范落地 ✅ 已完成

> **架构升级**：简历模板从硬编码管理升级为**规范驱动**的可扩展体系。引入 `template.json` 元数据规范，支持 display_name/description/layout/color_scheme/recommended_industries 等结构化描述；新增模板注册表 `registry.py`，支持子目录结构和扁平结构双模式发现；提供 `validate_template()` 校验工具；模板列表 API 改为从注册表动态生成。

| 功能 | 状态 | 说明 |
|------|------|------|
| template.json 元数据规范 | ✅ 已完成 | 支持 name/version/display_name/description/layout/color_scheme/recommended_industries/required_fields/optional_fields/supports_dark_mode/page_size/preview |
| 3 个模板添加 template.json | ✅ 已完成 | professional.json/academic.json/creative.json |
| TemplateMeta 数据模型 | ✅ 已完成 | 统一元数据模型，支持 validate() 校验 |
| 模板注册表 registry.py | ✅ 已完成 | 支持子目录结构和扁平结构双模式发现、list_templates/get_template_info/get_template_html_path/validate_template |
| 模板列表 API 动态化 | ✅ 已完成 | GET /api/resume/templates 改为从注册表动态生成，返回完整元数据 |
| 模板校验工具 | ✅ 已完成 | validate_template() 校验 HTML 文件、template.json 格式、变量引用、@page 规则 |
| get_available_templates() | ✅ 已完成 | resume_renderer.py 新增动态模板列表函数 |

**关键文件**：
- `resume_agent/templates/registry.py` — 新增：模板注册表 + 元数据管理 + 校验
- `resume_agent/templates/professional.json` — 新增：professional 模板元数据
- `resume_agent/templates/academic.json` — 新增：academic 模板元数据
- `resume_agent/templates/creative.json` — 新增：creative 模板元数据
- `resume_agent/resume_renderer.py` — 新增 get_available_templates()
- `backend/routes/resume.py` — 模板列表 API 改用注册表

### P1-8：提示词外置为模板文件 + 用户自定义指令注入点 ✅ 已完成

> **架构升级**：系统提示词从 Python 硬编码字符串外置为 Markdown 模板文件，支持版本管理和可视化编辑；新增用户自定义指令注入点，用户可通过 `custom_instructions.md` 注入个性化规则到系统提示词。

| 功能 | 状态 | 说明 |
|------|------|------|
| 系统提示词外置 | ✅ 已完成 | `system_prompt.py` 的硬编码字符串改为从 `prompts/system_prompt.md` 文件加载 |
| 用户自定义指令注入 | ✅ 已完成 | `build_resume_system_prompt()` 新增第 6 步：加载 `custom_instructions.md` 注入到"用户自定义规则"段 |
| load_user_instructions() | ✅ 已完成 | 从用户记忆目录读取 `custom_instructions.md`，注入系统提示词 |
| memory 白名单扩展 | ✅ 已完成 | `WRITABLE_MEMORY_FILES` 新增 `custom_instructions.md`，用户可通过 memory API 管理 |

**关键文件**：
- `resume_agent/prompts/system_prompt.md` — 新增：系统提示词主模板（从 Python 代码提取）
- `resume_agent/prompts/system_prompt.py` — 重构：从 .md 加载模板 + 新增用户自定义指令注入
- `resume_agent/memory/manager.py` — WRITABLE_MEMORY_FILES 新增 custom_instructions.md

### P1-9：配置供应商中立化 ✅ 已完成

> **架构升级**：LLM 配置从 DeepSeek 供应商锁定改为供应商中立。环境变量支持 `LLM_API_KEY`/`LLM_BASE_URL`/`LLM_MODEL`（优先），同时保留 `DEEPSEEK_*` 向后兼容；`.env.example` 更新为供应商中立的配置说明。

| 功能 | 状态 | 说明 |
|------|------|------|
| LLM_API_KEY 环境变量 | ✅ 已完成 | 优先于 DEEPSEEK_API_KEY，支持 OpenAI/DeepSeek/任何兼容接口 |
| LLM_BASE_URL 环境变量 | ✅ 已完成 | 优先于 DEEPSEEK_BASE_URL |
| LLM_MODEL 环境变量 | ✅ 已完成 | 优先于 DEEPSEEK_MODEL |
| 向后兼容 | ✅ 已完成 | DEEPSEEK_* 环境变量仍可用，LLM_* 优先级更高 |
| .env.example 更新 | ✅ 已完成 | 新增 LLM_* 配置说明，保留 DEEPSEEK_* 示例 |

**关键文件**：
- `resume_agent/config/settings.py` — effective_api_keys/effective_base_url/effective_model 属性支持 LLM_* 优先
- `.env.example` — 更新为供应商中立配置说明

### P1-10：用户设置页完善 ✅ 已完成

> **功能完善**：前端设置页新增"简历偏好"卡片，支持默认模板选择（单选卡片式）、语言风格切换（专业正式/轻松自然/学术严谨）、输出语言选择（中文/英文）、自动保存开关。所有偏好通过 `PUT /api/settings` 持久化到后端。

| 功能 | 状态 | 说明 |
|------|------|------|
| 默认模板选择 | ✅ 已完成 | 单选卡片式 UI，3 个模板带描述 |
| 语言风格切换 | ✅ 已完成 | 按钮组：专业正式/轻松自然/学术严谨 |
| 输出语言选择 | ✅ 已完成 | 按钮组：中文/英文 |
| 自动保存开关 | ✅ 已完成 | 切换按钮控制简历快照自动保存 |
| 偏好保存 | ✅ 已完成 | 保存按钮调用 PUT /api/settings 持久化 |

**关键文件**：
- `frontend/src/app/(app)/settings/page.tsx` — 新增简历偏好卡片

### P1-11：路由/消息/面板添加过渡动画 ✅ 已完成

> **体验提升**：为页面切换、消息气泡、评分卡片等交互元素添加过渡动画，提升视觉流畅度。使用 Tailwind CSS animate（tailwindcss-animate）的 `animate-in`/`fade-in`/`slide-in-from-bottom` 类实现轻量级 CSS 动画。

| 功能 | 状态 | 说明 |
|------|------|------|
| 页面切换淡入 | ✅ 已完成 | 主内容区 `animate-in fade-in-0 duration-200` |
| 消息气泡滑入 | ✅ 已完成 | MessageBubble `slide-in-from-bottom-2 duration-200` |
| 评分卡片滑入 | ✅ 已完成 | ResumeScoreCard `slide-in-from-bottom-3 duration-300` |

**关键文件**：
- `frontend/src/app/(app)/layout.tsx` — 主内容区添加页面切换淡入动画
- `frontend/src/app/(app)/chat/page.tsx` — 消息气泡添加滑入动画
- `frontend/src/components/resume-score-card.tsx` — 评分卡片添加滑入动画

---

## 测试覆盖

| 类型 | 文件 | 说明 |
|------|------|------|
| 单元测试 | `tests/test_runtime.py` | 16 个测试用例，覆盖包导入、配置、异常、ApiKeyPool、SessionPool、SSE 事件、API Schema、工具导入、提示词、记忆、API 配置校验 |
| 测试数据初始化 | `scripts/seed_test_data.py` | 创建用户目录 + 示例简历 + 记忆文件 + 简历快照 + 用户配置 |

**测试不足**：当前仅覆盖导入和基本功能验证，缺乏集成测试（对话流程、工具执行、API 调用等）。

---

## 项目配置文件

| 文件 | 说明 |
|------|------|
| `pyproject.toml` | 项目元数据 + 依赖 + 构建配置（hatchling） |
| `requirements.txt` | pip 依赖清单（含 fpdf2、markdown 渲染依赖） |
| `.env.example` | 环境变量模板（DEEPSEEK_API_KEY 等） |
