# 缺陷修复记录

| 序号 | 日期 | 缺陷描述 | 修复方式 | 涉及文件 |
|------|------|----------|----------|----------|
| 1 | 2026-04-19 | 创建会话失败：RuntimeBundle.__init__() missing 1 required positional argument: 'session_id' | `build_resume_runtime()` 构造 `RuntimeBundle` 时漏传 `session_id` 参数，补上 `session_id=sid` | `ResumeHarness/resume_agent/runtime.py` |
| 2 | 2026-04-19 | web_fetch 工具误调用虚构 URL (https://example.com/resume-optimization-history) 导致 HTTP 404 | LLM 将 memory_write 的"优化历史"功能与 web_fetch 混淆，在系统提示词中新增"工具使用规范"段落，明确区分 web_fetch（仅抓取外部网页）和 memory_write（写入本地记忆）的使用场景 | `ResumeHarness/resume_agent/prompts/system_prompt.py` |
| 3 | 2026-04-19 | 记忆文档不会自动更新：`MemoryWriteTool` 未注册到 ToolRegistry，且 `tool_metadata` 缺少 `user_id` | 1. 在 `_get_shared_tool_registry()` 中注册 `MemoryWriteTool`；2. 将 `tool_metadata` 从空字典改为 `{"user_id": user_id}`，确保工具执行时能获取当前用户 ID | `ResumeHarness/resume_agent/runtime.py` |
| 4 | 2026-04-21 | `SkillLoaderTool` 未在 ToolRegistry 中注册，LLM 无法调用 skill_loader 工具 | 在 `_get_shared_tool_registry()` 中添加 `from resume_agent.tools.skill_loader import SkillLoaderTool` 和 `_shared_tool_registry.register(SkillLoaderTool())` | `ResumeHarness/resume_agent/runtime.py` |
| 5 | 2026-04-21 | `ApiKeyPool` 未与 `OpenAICompatibleClient` 集成，多 Key 轮询功能空转 | 1. `OpenAICompatibleClient.__init__` 新增 `key_pool` 参数；2. `stream_message` 每次请求前从 pool 获取 key 并重建 client；3. 收到 429 时调用 `key_pool.report_429()`；4. `_get_shared_api_client` 在多 Key 时传入 pool | `ResumeHarness/resume_agent/api/openai_client.py` `ResumeHarness/resume_agent/runtime.py` |
| 6 | 2026-04-21 | `session_pool._save_snapshot()` 空实现，会话淘汰/关闭时不持久化对话历史 | 实现 `_save_snapshot()` 方法，调用 `save_session_snapshot()` 将引擎消息、用量、模型等信息持久化到磁盘 | `ResumeHarness/resume_agent/session_pool.py` |
| 7 | 2026-04-21 | 上下文压缩（compact）未实现，长对话可能超出 Token 限制 | 新建 `services/compact.py`，实现 Token 估算、阈值判断、LLM 摘要生成；在 `run_query()` 中集成自动压缩，消息接近阈值时自动触发 | `ResumeHarness/resume_agent/services/compact.py` `ResumeHarness/resume_agent/engine/query.py` |
| 8 | 2026-04-21 | SSE 心跳未实现，长连接可能被中间件超时断开 | 在 `_chat_stream()` 中添加心跳逻辑，每 15 秒发送一次 `SsePing` 事件 | `ResumeHarness/backend/routes/chat.py` |
| 9 | 2026-04-21 | `render_pdf.py` 硬编码 Windows 字体路径，跨平台兼容性差 | 改为跨平台字体搜索：根据 `platform.system()` 选择搜索目录（Windows/macOS/Linux），扩展字体候选列表（SimHei/NotoSansSC/WenQuanYiMicroHei/PingFangSC 等），支持子目录递归搜索 | `ResumeHarness/resume_agent/render_pdf.py` |
| 10 | 2026-04-21 | `web_fetch` 简易正则提取 HTML，复杂页面抓取效果不佳 | 改进为两层提取：1. 优先使用 readability-lxml 提取正文；2. 回退时先尝试 `<article>/<main>` 标签，再正则去除标签；同时去除 nav/footer/header 等非正文标签 | `ResumeHarness/resume_agent/tools/web_fetch.py` |
| 11 | 2026-05-01 | PDF 上传不支持解析，用户无法直接上传 PDF 简历 | 新增 `_extract_pdf_text()` 函数使用 PyPDF2 提取 PDF 文本，修改 `upload_resume` 端点支持 PDF 文件自动解析 | `ResumeHarness/backend/routes/memory.py` |
| 12 | 2026-05-02 | 对话页回车不会发送消息 | `handleKeyDown` 绑定 Ctrl+Enter 发送，改为 Enter 直接发送、Shift+Enter 换行（聊天产品通用模式），同步更新 placeholder 提示 | `frontend/src/app/(app)/chat/page.tsx` |
| 13 | 2026-05-02 | 对话页消息过长时不会自动滚动 | Radix UI `ScrollArea` 的 ref 无法直接操控内部 viewport 滚动，改为 `overflow-y-auto` div + `scrollIntoView` 锚点方案 | `frontend/src/app/(app)/chat/page.tsx` |
| 14 | 2026-05-02 | 对话中已给出 JD 描述，下次仍调用 web_fetch 重复获取 | 1. 后端新增 `session_started` SSE 事件，前端收到后存储 session_id，下次请求复用同一会话保持上下文连续；2. 系统提示词加强约束：用户已提供 JD 内容时不重复调用 web_fetch，应通过 memory_write 持久化 | `ResumeHarness/backend/routes/chat.py` `ResumeHarness/resume_agent/prompts/system_prompt.py` `ResumeHarness/resume_agent/models/sse_events.py` `frontend/src/app/(app)/chat/page.tsx` |
| 15 | 2026-05-02 | 对话页没有展示思考过程 | 后端新增 `ThinkingDelta` 事件类型：openai_client 流式输出 `reasoning_content` → query.py 传递 `ThinkingDelta` → chat.py 序列化为 `SseThinkingDelta`；前端在消息气泡中增加可折叠"思考过程"区域 | `ResumeHarness/resume_agent/api/client.py` `ResumeHarness/resume_agent/api/openai_client.py` `ResumeHarness/resume_agent/engine/stream_events.py` `ResumeHarness/resume_agent/engine/query.py` `ResumeHarness/resume_agent/models/sse_events.py` `ResumeHarness/backend/routes/chat.py` `frontend/src/store/chat.ts` `frontend/src/app/(app)/chat/page.tsx` |
| 16 | 2026-05-02 | 对话页没有头像 | 在 `MessageBubble` 组件中添加头像：助手侧 Bot 图标（蓝底），用户侧 User 图标（灰底） | `frontend/src/app/(app)/chat/page.tsx` |
| 17 | 2026-05-02 | 页面刷新后对话内容丢失，不支持历史会话查看 | 1. chatStore 添加 zustand persist 中间件，持久化 sessionId 和 messages 到 localStorage；2. 聊天页顶栏新增"历史"按钮，通过 Sheet 抽屉展示历史会话列表（调用 `GET /api/sessions`）；3. 点击历史会话加载详情（调用 `GET /api/sessions/{id}`），转换消息格式后恢复到当前视图 | `frontend/src/store/chat.ts` `frontend/src/app/(app)/chat/page.tsx` `frontend/src/lib/api.ts` |
| 18 | 2026-05-02 | 生成的简历中包含无关的思考过程内容 | `_extract_resume_content` 匹配到简历结构后返回整个文本（含前面的思考文字），改为从正则匹配的起始位置截取，只保留简历 Markdown 部分 | `ResumeHarness/backend/routes/chat.py` |
| 19 | 2026-05-02 | 左侧新建对话按钮不生效，与右上角新对话逻辑不一致 | 侧边栏"新建对话"按钮只做了 `router.push("/chat")`，未清空 chatStore。改为同时调用 `useChatStore.getState().clearMessages()` 清空消息和 sessionId | `frontend/src/app/(app)/layout.tsx` |
| 20 | 2026-05-02 | 路由始终为 /chat，刷新后无法恢复会话 | 1. 收到 `session_started` 事件时通过 `router.replace` 更新 URL 为 `/chat?sid=xxx`；2. 页面加载时从 URL `sid` 参数恢复会话并加载历史消息；3. 历史会话列表点击也更新 URL | `frontend/src/app/(app)/chat/page.tsx` |
| 21 | 2026-05-02 | 生成简历快照中包含优化说明等非简历内容 | 后端 `_extract_resume_content` 新增非简历章节标题检测正则（优化说明/修改建议等），匹配到后截断简历内容 | `ResumeHarness/backend/routes/chat.py` |
| 22 | 2026-05-02 | 发送消息时出现两个 Bot 头像 | `handleSend()` 预添加空 assistant 消息，`MessageBubble` 和"思考中..."同时渲染了 Bot 头像。改为流式传输中跳过空 assistant 消息的渲染，仅由"思考中..."替代 | `frontend/src/app/(app)/chat/page.tsx` |
| 23 | 2026-05-02 | 简历内容截取逻辑错误：正则 `^#\s+.+\n.*关键词` 可能匹配到分析章节而非简历标题 | 重写简历提取策略：1. 先搜索 `## (个人简介|工作经历|...)` 定位简历二级章节；2. 从该章节往前找最近的 `# 姓名` 级标题作为起点；3. 截断后面的 `## 优化说明` 等非简历章节 | `ResumeHarness/backend/routes/chat.py` |
| 24 | 2026-05-02 | 思考过程未正确折叠展示，流式传输中缺乏思考状态反馈 | 1. `ThinkingSection` 新增 `streaming` prop，流式传输中自动展开并显示加载动画；2. `MessageBubble` 传递 `isStreaming` 给 `ThinkingSection`；3. 注：`deepseek-chat` 不返回 `reasoning_content`，需使用推理模型（如 `deepseek-reasoner`）才有思考过程数据 | `frontend/src/app/(app)/chat/page.tsx` |
| 25 | 2026-05-02 | 服务重启/会话淘汰后重新访问，LLM 无法感知之前的对话内容 | 1. `session_storage.py` 新增 `load_session_snapshot()` 按 session_id 加载指定快照；2. `session_pool.py` 的 `get_or_create()` 创建新 bundle 后调用 `_restore_history()`，从磁盘快照恢复历史消息到 `QueryEngine`，使 LLM 能看到完整对话上下文 | `ResumeHarness/resume_agent/services/session_storage.py` `ResumeHarness/resume_agent/session_pool.py` |
| 26 | 2026-05-03 | 简历渲染包含"优化要点"等非简历内容，黑名单截断无法穷举 LLM 输出变体 | 1. 系统提示词要求 LLM 用 `<!--RESUME-->` / `<!--/RESUME-->` 标记包裹简历正文；2. `_extract_resume_content()` 从黑名单改为双路径：标记提取（主路径）+ 白名单过滤（降级路径）；3. 新增 `_StreamingMarkerFilter` 在流式推送时自动过滤标记标签，用户看不到标记 | `ResumeHarness/resume_agent/prompts/system_prompt.py` `ResumeHarness/backend/routes/chat.py` |
| 27 | 2026-05-03 | 简历预览后"优化要点"不可见，被 ResumePreview 组件替代 | 1. `SseResumeData` 新增 `suggestions` 字段，后端提取标记外建议内容；2. 前端在 ResumePreview 下方以 Markdown 形式展示优化建议；3. `SseResumeData` 新增 `resume_prefix` 字段，前端三段式渲染：前缀 Markdown → 预览组件 → 后缀 Markdown，只有简历部分被替换，其他文字保留原位 | `ResumeHarness/resume_agent/models/sse_events.py` `ResumeHarness/backend/routes/chat.py` `frontend/src/store/chat.ts` `frontend/src/app/(app)/chat/page.tsx` |
| 28 | 2026-05-03 | 简历预览只展示左栏布局，不支持切换模板类型，template_hint 硬编码为 professional | 前端 MessageBubble 新增模板切换按钮组（商务/学术/创意），`activeTemplate` 状态控制 ResumePreview 渲染，默认跟随后端 template_hint | `frontend/src/app/(app)/chat/page.tsx` |
| 34 | 2026-05-04 | `aiosqlite` 未安装到虚拟环境导致 ModuleNotFoundError | `.venv\Scripts\pip install aiosqlite` 安装缺失依赖 | `ResumeHarness/requirements.txt` |
| 35 | 2026-05-04 | `ApiRetryEvent` 在 `stream_events.py` 中未定义导致 ImportError | `chat.py` 导入了不存在的 `ApiRetryEvent`，移除该导入 | `ResumeHarness/backend/routes/chat.py` |
| 36 | 2026-05-04 | 前端 `prevResumeContent` 未解构导致 ReferenceError | `MessageBubble` 组件 props 类型中定义了 `prevResumeContent` 但解构时遗漏，补充到解构列表 | `frontend/src/app/(app)/chat/page.tsx` |
| 37 | 2026-05-04 | 前端从局域网 IP 访问时 CORS 拒绝请求 | 1. `settings.py` 默认 CORS 白名单扩展；2. 新增 `CORS_ALLOWED_ORIGINS` 环境变量覆盖；3. `.env` 配置 `CORS_ALLOWED_ORIGINS=*` | `ResumeHarness/resume_agent/config/settings.py` `ResumeHarness/.env` |
| 38 | 2026-05-04 | 登录接口 `/api/auth/login` 请求 pending 无响应 | Starlette `BaseHTTPMiddleware` 多层堆叠时 body 消费导致死锁，将 `AuthMiddleware`/`RateLimitMiddleware`/`MonitoringMiddleware` 全部改为纯 ASGI 中间件 | `ResumeHarness/backend/middleware/auth.py` `ResumeHarness/backend/middleware/rate_limit.py` `ResumeHarness/backend/middleware/monitoring.py` `ResumeHarness/backend/app.py` |
| 39 | 2026-05-05 | 简历保存接口失败：PUT `/api/memory/简历原文.md` 返回 400 | 后端 `PUT /api/memory/{doc_name}` 明确禁止直接编辑 `简历原文.md`（需通过 upload 接口），前端 `handleSave` 对所有文件统一用 PUT。修改前端：保存 `简历原文.md` 时将编辑内容构造为 File 通过 `POST /api/memory/upload` 上传 | `frontend/src/app/(app)/memory/page.tsx` |
| 40 | 2026-05-05 | 对话输出到一半暂停 | `max_tokens=4096` 太小，简历生成时被截断（`finish_reason=length`），客户端无续写机制。1. `_stream_once` 新增自动续写：检测 `finish_reason=length` 时追加"请继续"消息继续请求（最多 5 次）；2. 默认 `max_tokens` 从 4096 提升至 8192 | `ResumeHarness/resume_agent/api/openai_client.py` `ResumeHarness/resume_agent/config/settings.py` |
| 41 | 2026-05-06 | 记忆页面预览模式不支持滚动 | `ScrollArea` 在 flex-1 布局下高度约束失效，改用原生 `overflow-y-auto` + `min-h-0` | `frontend/src/app/(app)/memory/page.tsx` |
| 42 | 2026-05-06 | 记忆页面编辑模式滚动样式错乱 | Textarea 嵌套在 ScrollArea 内滚动冲突，编辑模式改为 Textarea 直接占满 flex-1 区域 + resize-none | `frontend/src/app/(app)/memory/page.tsx` |
| 43 | 2026-05-06 | 对话页复制按钮位置不合理且复制失败 | 1. 复制按钮从绝对定位改为正常流布局（气泡末尾）；2. 增加 `document.execCommand("copy")` fallback 兼容非 HTTPS 环境 | `frontend/src/app/(app)/chat/page.tsx` |
| 44 | 2026-05-06 | 简历编辑保存成功但前端提示失败 | `createAuthApi` 返回 ofetch 实例自动解析 JSON，代码又调 `.json()` 导致 TypeError。改为直接使用 ofetch 返回值，不再手动 `.json()` | `frontend/src/app/(app)/resumes/[id]/page.tsx` |
| 45 | 2026-05-06 | 简历编辑无法新增列表/模块 | ResumePreview 编辑模式下仅渲染已有内容，无添加入口。1. 各章节条件改为 `length > 0 \|\| editable`，编辑模式始终显示章节；2. 每个列表末尾添加 `+ 添加` 按钮（工作经历/教育/技能/项目/亮点/成就/贡献）；3. `buildSectionOrder` 传入 `editable` 参数，编辑模式下显示空章节 | `frontend/src/components/resume-preview.tsx` |
| 46 | 2026-05-06 | DOCX 下载失败 | `download_resume` 路由中 format=docx 时先经过 `render_resume()`，该函数不支持 docx 格式抛出异常。将 docx 分支提前到 `render_resume` 之前 | `ResumeHarness/backend/routes/resume.py` |
| 47 | 2026-05-06 | PDF 下载失败 | Next.js 16 默认 Turbopack，webpack 的 `canvas: false` 配置不生效。新增 `turbopack.resolveAlias` 配置 | `frontend/next.config.ts` |
| 48 | 2026-05-06 | 分享功能失败 | 与 #44 相同：分享 API 使用 `createAuthApi` 后又调 `.json()`；另外 `DropdownMenuTrigger`/`DialogTrigger` 的 `render` prop 用法不当。1. 修复 ofetch 调用方式；2. 将 Trigger 子元素改为直接包裹 Button | `frontend/src/app/(app)/resumes/[id]/page.tsx` |
| 49 | 2026-05-06 | HTML 预览未垂直居中且非 A4 纸大小 | 简历预览外层改为 `flex justify-center` + `w-[210mm] min-h-[297mm]` A4 纸尺寸容器，移除模板内部 `max-w-[800px] mx-auto shadow-lg rounded-lg` | `frontend/src/app/(app)/resumes/[id]/page.tsx` `frontend/src/components/resume-preview.tsx` |
| 50 | 2026-05-06 | button 嵌套 button 导致 hydration 报错 | `DropdownMenuTrigger` 和 `DialogTrigger` 默认渲染 `<button>`，子元素 `<Button>` 也是 `<button>`，改为使用 base-ui 的 `render` prop 将 Trigger 渲染为 Button 组件 | `frontend/src/app/(app)/resumes/[id]/page.tsx` |
| 51 | 2026-05-06 | PDF 下载无请求直接返回渲染失败 | `@react-pdf/renderer` 客户端 PDF 生成因字体/Canvas 兼容性问题持续失败。改为后端 API 下载 PDF（`/api/resume/{id}/download?format=pdf`），与 DOCX/HTML 下载方式一致 | `frontend/src/app/(app)/resumes/[id]/page.tsx` |
| 52 | 2026-05-06 | 简历编辑模式无法删除条目，空条目不会被自动清除 | 1. 每个条目（工作经历/教育/技能/项目）及子条目（亮点/成就/贡献）添加 × 删除按钮（`DeleteItemButton`）；2. 新增 `removeArrayItem` 和 `removeSubListItem` 辅助函数；3. `handleSaveEdit` 保存前调用 `cleanEmptyItems` 自动清除所有字段为空的条目和子条目 | `frontend/src/components/resume-preview.tsx` `frontend/src/app/(app)/resumes/[id]/page.tsx` |
| 53 | 2026-05-06 | 简历列表页删除按钮不生效，确认对话框无法弹出 | `Dialog` 的 `onOpenChange` 只处理关闭（`!open && setDeleteId(null)`），打开时 `open=true` 条件不满足导致 `deleteId` 不更新，对话框无法弹出。改为 `setDeleteId(open ? resume.resume_id : null)` | `frontend/src/app/(app)/resumes/page.tsx` |
| 54 | 2026-05-06 | 分享链接复制提示失败 | `navigator.clipboard.writeText()` 仅 HTTPS 安全上下文可用，HTTP 环境直接失败。创建 `lib/clipboard.ts` 统一工具函数，优先 clipboard API，降级 `execCommand("copy")`，全项目复用 | `frontend/src/lib/clipboard.ts` `frontend/src/app/(app)/resumes/[id]/page.tsx` `frontend/src/app/(app)/chat/page.tsx` |
| 55 | 2026-05-06 | PDF 生成效果与前端展示不一致 | 前后端模板独立实现导致风格偏差。1. 统一渲染源：重写后端 Jinja2 模板对齐前端 React 组件样式（Professional 侧边栏改 slate-800、Academic 增加竖线装饰+技能标签+时间徽章、Creative 增强卡片阴影+项目名加粗）；2. 前端模板从单文件拆分为独立模板文件+注册表 | `ResumeHarness/resume_agent/templates/*.html` `ResumeHarness/resume_agent/render_pdf.py` `ResumeHarness/resume_agent/render_docx.py` `frontend/src/components/templates/` |
| 56 | 2026-05-06 | 学术/创意模板样式不佳，缺少多种模板选择 | 1. 优化学术模板（左侧竖线装饰、技能标签、时间徽章）；2. 优化创意模板（项目名加粗、增强渐变和阴影）；3. 新增 4 套模板：极简（minimal）、优雅（elegant）、科技（tech）、紧凑（compact）；4. 简历详情页模板选择 UI 从文字按钮改为下拉菜单式（带颜色标识和描述）；5. 对话页模板切换从 3 个文字按钮改为下拉菜单式，支持全部 7 个模板 | `frontend/src/components/templates/` `frontend/src/components/templates/registry.tsx` `frontend/src/components/resume-preview.tsx` `frontend/src/app/(app)/resumes/[id]/page.tsx` `frontend/src/app/(app)/chat/page.tsx` `ResumeHarness/resume_agent/templates/` `ResumeHarness/resume_agent/resume_renderer.py` |
| 57 | 2026-05-07 | 商务模板PDF与Web样式不一致 | 1. professional.html 支持 section_order 动态章节顺序（Jinja2 模板循环渲染）；2. 侧边栏宽度从 72mm 改为 38% 对齐前端；3. 标题色 #2c3e50→#1e293b、装饰色 #3498db→#3b82f6、侧边栏标题色→#60a5fa 等；4. education 分隔符 ·→，对齐前端 | `ResumeHarness/resume_agent/templates/professional.html` `ResumeHarness/resume_agent/templates/professional.css` |
| 58 | 2026-05-07 | 分享链接HTML未页面居中 | `_render_html_from_resume_data` 和 `_render_html_from_markdown` 输出的 HTML body 添加 `display:flex; justify-content:center; background:#f5f5f5; padding:20px 0` 样式；新增 `render_resume_data_to_html_with_center()` 函数供 API 端点复用 | `ResumeHarness/resume_agent/resume_renderer.py` |
| 59 | 2026-05-07 | Web编辑后下载PDF内容未同步 | PDF/HTML/DOCX 下载和预览端点优先使用 `load_resume_data`（JSON）渲染，降级到 Markdown 渲染；确保 section_order 等编辑后数据正确传递到 Jinja2 模板 | `ResumeHarness/backend/routes/resume.py` |
| 60 | 2026-05-07 | 拖拽手柄大小模块混在一起 | SortableItem 新增 `handlePosition` 参数（left/right）；大模块（章节排序）手柄在左侧，小模块（条目排序）手柄在右侧；编辑模式下主内容区和侧边栏增加右侧 padding 为手柄留空间 | `frontend/src/components/sortable-section.tsx` `frontend/src/components/templates/professional.tsx` |
| 61 | 2026-05-07 | professional.html 模板字体大小和显示样式与 Web 端细微不一致 | 1. `.contact-icon` 颜色从 `#6b7280` 改为 `#9ca3af`（与前端 text-gray-400 一致）；2. 移除 `.contact-icon` 的 `font-family: "SimHei"...` 声明（中文字体导致 Unicode 图标 ✉☎📍🌐 渲染异常）；3. 列表项 bullet 定位方式从 `padding-left:12px + left:-12px` 改为 `padding-left:14px + left:0`（与前端 pl-3+before:left-0 布局对齐）；4. 新增缺失的 `.proj-period` 样式定义（`font-size:8.5pt; color:#64748b`，此前继承 body 的 10pt） | `ResumeHarness/resume_agent/templates/professional.html` |
